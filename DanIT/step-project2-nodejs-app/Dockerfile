# Використовуємо офіційний Node.js образ з версією 14
FROM node:14

# Встановлюємо робочу директорію всередині контейнера
WORKDIR /app

# Копіюємо package.json та package-lock.json (якщо він є)
COPY package*.json ./

# Встановлюємо залежності проекту
RUN npm install

# Копіюємо весь проект у робочу директорію
COPY . .

# Відкриваємо порт 3000 для доступу до програми
EXPOSE 3000

# Додаємо Jest у якості залежності для тестування (якщо він ще не встановлений)
RUN npm install --save-dev jest

# За замовчуванням запускаємо додаток
CMD ["npm", "start"]
